# This file anchors ${maven.multiModuleProjectDirectory} to the repo root,
# so that pom.xml can reference root-level files (e.g. jvm.options) with a
# path that resolves correctly from any module in the build.
