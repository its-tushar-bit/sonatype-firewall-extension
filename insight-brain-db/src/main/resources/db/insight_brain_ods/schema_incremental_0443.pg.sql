-- SaaS Compatible

-- PostgreSQL already applied the repository_ancestor fix in schema_incremental_0442.pg.sql.
-- This script exists only to pair with schema_incremental_0443.h2.sql (which contains the
-- H2-compatible version of the same fix).
SELECT 1;
