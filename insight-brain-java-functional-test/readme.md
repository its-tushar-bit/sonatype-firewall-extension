# Java Functional Tests #

Uses Selenide, currently built in sonatype/selenide-shaded due to version conflicts with the guava dependency.

## Chrome ##

`-Dselenide.browser=chrome -Dwebdriver.chrome.driver=C:\IDE\chromedriver_win32\chromedriver.exe`

## PhantomJS ##

`-Dbrowser=phantomjs`