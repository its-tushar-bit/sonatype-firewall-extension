saasAddress: "https://clm-staging.sonatype.com/"
#saasAddress: "http://localhost:8080/insight-portal"

# Directory for data files.
sonatypeWork: ./target/sonatype-work/clm-server

logging:
  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: DEBUG
  loggers:
    "eu.medsea.mimeutil.MimeUtil2": INFO
    "org.apache.http": INFO
    "org.eclipse.jetty": INFO
    "com.ning.http.client": INFO
    "org.springframework.jdbc.datasource.init.ResourceDatabasePopulator": INFO
  console:
    logFormat: "%date %level [%thread%X{DC}] %logger - %msg%n"

# HTTP-specific options.
http:

  # The port on which the HTTP server listens for service requests.
  # Because Java cannot drop privileges in a POSIX system, these
  # ports cannot be in the range 1-1024. A port value of 0 will
  # make the OS use an arbitrary unused port.
  port: 10070

  # The port on which the HTTP server listens for administrative
  # requests. Subject to the same limitations as "port".
  adminPort: 10071