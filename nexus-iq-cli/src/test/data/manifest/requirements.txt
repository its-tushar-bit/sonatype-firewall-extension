# Comment at the beginning of the file
foo1==1.1
#Comment inside the file
 # Comment prefixed by space
	# Comment prefixed by tab
foo2==1.2 # Comment prefixed by space on a dependency line
foo3==1.3	# Comment prefixed by tab on a dependency line
foo4==\ # Comment after line continuation (comments are removed before line continuations are processed)
1.4
foo5==1.5
https://example.com/page#egg=foo-1.6  # Comment on a line that contains another '#' character
# Comment at the end of the file
